{
  "name": "ufc-offline-viewer",
  "version": "0.7.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/file-protocol-build.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  },
  "dependencies": {
    "jszip": "^3.10.2"
  },
  "description": "Offline static viewer for UFC / CIM content (IndexedDB + import/packs only; no live API)"
}
